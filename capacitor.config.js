/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.historio.app',
  appName: 'Historio',
  webDir: 'build',
  resources: {
    icon: 'public/icon512.png',
    splash: 'public/icon512.png'
  }
};

export default config;